#include <stdio.h>

int main(int argc, char* argv[]) {

    int numRocks, numActions;
    int changeType, par1, par2, par3;

    while (scanf("%i %i", &numRocks, &numActions) != EOF) {
    	bool rocks[numRocks]; // 0 is white, 1 is black

    	for (int i = 0; i < numActions; ++i) {

    	}
    }

    return 0;
}
